<% if @calendar_entry.errors.present? %>
    setTimeout(function(){
        Heyday.UI.showModal('New Event',"<%= j(render 'form') %>");
        Heyday.UI.initJSElements();
    },550);
<% else %>
    Heyday.reload();
<% end %>