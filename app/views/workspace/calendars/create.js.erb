<% if @calendar.errors.present? %>
    $('.ui.modal').parent().remove();
    Heyday.UI.showModal('<%=current_user.name%> Calendar','<%= j render(partial: 'form') %>');
    Heyday.UI.initJSElements();
<% else %>
    Heyday.reload();
<% end %>