<% if @calendar.errors.present? %>
    $('.ui.modal').parent().remove();
    Heyday.UI.showModal('<%=@calendar.user.name%> Calendar','<%= j render(partial: 'form') %>');
    Heyday.UI.initJSElements();
<% else %>
    Heyday.reload();
<% end %>